# Default Environment Variables for @johnqh/mail-box-components
# These are the default values used by the project

# === DEPENDENCY MANAGEMENT ===
# Controls which version of @johnqh/design-system to use
# false = Load from npm registry (production/CI default)
# true = Load from local filesystem (development)
USE_LOCAL_LIB=false

# === BUILD CONFIGURATION ===
# Node environment for builds
NODE_ENV=production

# === TESTING CONFIGURATION ===
# Test environment setup
VITEST_ENV=jsdom

# === DEVELOPMENT SERVER ===
# Port for development server (if using vite dev server)
PORT=3000
HOST=localhost